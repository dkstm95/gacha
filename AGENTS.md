# Gacha Agent Instructions

Use the repository-wide agent policy in:

```text
docs/agent-policy.md
```

That document is the source of truth for quality checks, verification, git
automation, and release automation. Do not duplicate those rules here.
